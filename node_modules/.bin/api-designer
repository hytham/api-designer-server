#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../api-designer/bin/api-designer.js" "$@"
  ret=$?
else 
  node  "$basedir/../api-designer/bin/api-designer.js" "$@"
  ret=$?
fi
exit $ret
